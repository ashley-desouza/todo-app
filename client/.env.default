# Server API URL.
# Vue CLI only exposes environment variables prefixed with VUE_APP_.
# Local: the Express server's default port (3000)
# Production: deployed server URL (For Example: https://todo-app.onrender.com)
VUE_APP_API_URL=http://localhost:3000