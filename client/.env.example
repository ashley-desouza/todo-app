# Backend API URL.
# Vue CLI only exposes env vars prefixed with VUE_APP_.
# Local dev: matches the Express server's default port (3000)
# Production: deployed backend URL (e.g., https://todo-app.onrender.com)
VUE_APP_API_URL=http://localhost:3000